@if (!$queryNull)
<a href="{{ route('supports.index') }}" class="flex-shrink-0 font-semibold border-transparent border-4 text-red-500 hover:text-red-800 text-sm py-1 rounded" type="button">
    Limpar 
</a>
@endif
